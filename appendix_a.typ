#import "template.typ": * // если внутри приложения нужны таблицы, код или примечания

Оформление как в ГОСТ: статус на второй строке, а заголовок приложения на третьей!

```bash
sudo apt update
sudo apt install -y build-essential
```
